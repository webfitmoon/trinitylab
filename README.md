# trinitylab

Next.js App Router + React.

```sh
npm ci
npm run dev
npm run build
```

Vercel: Next.js preset, repository root, build command `npm run build`.

The approved demo content and noindex setting are preserved. Media lives in `public/media`.
